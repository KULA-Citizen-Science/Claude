// Intentionally empty: subprojects declare their own plugins with explicit versions
// (see app/build.gradle.kts, core/build.gradle.kts) so that `:core` — a plain Kotlin/JVM
// module — can be configured and tested without ever resolving the Android Gradle Plugin.
